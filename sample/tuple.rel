int main() {
        «int, int, real» x;
        x[1] = 3;
        x = «x[1] + 3, 2, x[0]»;
}

«int, int» foo(«int, real» p, real z) {
        [1, 4  ],
        [2, 5.6]
};

rel Z(int x, real y);

Z : x != 5 && y != 6.7;

rel (int n) range(int i, int f) {
        rel (int n) ret = {};
        for i in (i; f) {
                ret = ret <- {[i]};
        }
        return ret;
}

for y in range(1, 3)@n->i {
        
}
